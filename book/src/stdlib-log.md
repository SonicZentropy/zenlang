# Logging

The `std::log` module provides leveled logging.

```rust
use std::log::*;
```

## Functions

```rust
info(msg)    // Info level
warn(msg)    // Warning level
error(msg)   // Error level
debug(msg)   // Debug level
```
