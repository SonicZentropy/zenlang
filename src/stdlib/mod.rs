// Phase 9: Standard library built-in functions
// TODO: Register built-in functions (print, assert, type_of, int, float, etc.)

use crate::error::Result;
use crate::vm::VM;

pub fn register_builtins(_vm: &mut VM) -> Result<()> {
    // Placeholder
    Ok(())
}
