// Zenlang prelude — all iterator adapters (`map`, `filter`, `fold`, `collect`,
// etc.) are now implemented as native Rust functions in `src/stdlib/iter.rs`
// because creating `ForeignObject`s requires access to `VM::foreigns.insert()`,
// which is not possible from Zen code.  The closures are invoked via
// `ctx.call_value()`.
