enum Option {
    Some(int),
    None,
}

fn main() {
    let x = Some(42);
    let y = None;

    match x {
        Some(v) => assert(v == 42),
        None => assert(false),
    }

    match y {
        Some(_v) => assert(false),
        None => assert(true),
    }
}
